/* General Styles */
body {
    font-family: 'Arial', sans-serif;
    margin: 0;
    padding: 0;
    background: #f0f4f8;
    color: #333;
}

/* Main Container */
.container {
    display: flex;
    justify-content: center;
    align-items: center;
    padding: 20px;
    background: linear-gradient(135deg, #6a1b9a, #8e24aa);
    min-height: 100vh;
}

/* Profile Cards */
.profiles {
    display: grid;
    grid-template-columns: repeat(2, 1fr); /* Two profiles per row */
    gap: 30px;
    width: 100%;
    max-width: 1400px;
    height: auto;
    padding: 20px;
}

/* Profile Card */
.profile-card {
    background: linear-gradient(135deg, #ff9800, #ff5722);
    border-radius: 15px;
    box-shadow: 0 15px 25px rgba(0, 0, 0, 0.1);
    padding: 30px;
    text-align: center;
    transition: transform 0.3s ease-in-out;
}

.profile-card:hover {
    transform: translateY(-20px);
}

.profile-img img {
    width: 250px;
    height: 250px;
    object-fit: cover;
    border-radius: 50%;
    border: 5px solid #fff;
}

.profile-info {
    padding: 20px 0;
}

.profile-info h2 {
    font-size: 30px;
    margin: 10px 0;
    color: #fff;
}

.profile-info p {
    font-size: 18px;
    color: #f0f0f0;
    margin: 10px 0;
}

.show-more-btn {
    display: inline-block;
    background-color: #fff;
    color: #ff5722;
    padding: 12px 25px;
    margin-top: 20px;
    text-decoration: none;
    border-radius: 25px;
    font-weight: bold;
    transition: background-color 0.3s ease, color 0.3s ease;
}

.show-more-btn:hover {
    background-color: #ff5722;
    color: #fff;
}

/* Profile Detail Page */
.profile-detail-container {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    background: linear-gradient(135deg, #6a1b9a, #8e24aa);
    padding: 20px;
}

.profile-detail-card {
    background: linear-gradient(135deg, #ff9800, #ff5722);
    border-radius: 15px;
    box-shadow: 0 15px 25px rgba(0, 0, 0, 0.2);
    padding: 40px 50px;
    text-align: center;
    width: 400px;
    height: 550px;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    transition: transform 0.3s ease-in-out;
}

.profile-detail-card:hover {
    transform: scale(1.05);
}

.profile-detail-img img {
    width: 250px;
    height: 250px;
    object-fit: cover;
    border-radius: 50%;
    border: 5px solid white;
    margin-bottom: 20px;
}

.profile-name {
    font-size: 35px;
    color: #fff;
    margin-bottom: 15px;
}

.profile-detail {
    font-size: 18px;
    color: #f0f0f0;
    margin: 10px 0;
}

.back-btn {
    display: inline-block;
    background-color: #fff;
    color: #ff5722;
    padding: 12px 25px;
    margin-top: 20px;
    text-decoration: none;
    border-radius: 25px;
    font-weight: bold;
    transition: background-color 0.3s ease, color 0.3s ease;
}

.back-btn:hover {
    background-color: #ff5722;
    color: #fff;
}

/* Responsive Design */
@media (max-width: 768px) {
    .profiles {
        grid-template-columns: 1fr; /* One profile per row on smaller screens */
    }

    .profile-detail-card {
        width: 90%;
        height: auto;
    }

    .profile-detail-img img {
        width: 200px;
        height: 200px;
    }
}


/* General Styles */
body {
    font-family: 'Arial', sans-serif;
    margin: 0;
    padding: 0;
    background: #f7f8fa;
    color: #333;
}

/* Main Container */
.container {
    display: flex;
    justify-content: center;
    align-items: center;
    padding: 20px;
    background: linear-gradient(135deg, #36d1dc, #5b86e5);
    min-height: 100vh;
}

/* Profile Cards */
.profiles {
    display: grid;
    grid-template-columns: repeat(2, 1fr); /* Two profiles per row */
    gap: 30px;
    width: 100%;
    max-width: 1400px;
    height: auto;
    padding: 20px;
}

/* Profile Card */
.profile-card {
    background: linear-gradient(135deg, #ff6a00, #ffb199);
    border-radius: 15px;
    box-shadow: 0 15px 25px rgba(0, 0, 0, 0.1);
    padding: 30px;
    text-align: center;
    transition: transform 0.3s ease-in-out;
}

.profile-card:hover {
    transform: translateY(-20px);
}

.profile-img img {
    width: 250px;
    height: 250px;
    object-fit: cover;
    border-radius: 50%;
    border: 5px solid #fff;
}

.profile-info {
    padding: 20px 0;
}

.profile-info h2 {
    font-size: 30px;
    margin: 10px 0;
    color: #fff;
}

.profile-info p {
    font-size: 18px;
    color: #f0f0f0;
    margin: 10px 0;
}

.show-more-btn {
    display: inline-block;
    background-color: #ffffff;
    color: #ff6a00;
    padding: 12px 25px;
    margin-top: 20px;
    text-decoration: none;
    border-radius: 25px;
    font-weight: bold;
    transition: background-color 0.3s ease, color 0.3s ease;
}

.show-more-btn:hover {
    background-color: #ffb199;
    color: white;
}

/* Profile Detail Page */
.profile-detail-container {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    background: linear-gradient(135deg, #36d1dc, #5b86e5);
    padding: 20px;
}

.profile-detail-card {
    background: linear-gradient(135deg, #ff6a00, #ffb199);
    border-radius: 15px;
    box-shadow: 0 15px 25px rgba(0, 0, 0, 0.2);
    padding: 40px 50px;
    text-align: center;
    width: 400px;
    height: 550px;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    transition: transform 0.3s ease-in-out;
}

.profile-detail-card:hover {
    transform: scale(1.05);
}

.profile-detail-img img {
    width: 250px;
    height: 250px;
    object-fit: cover;
    border-radius: 50%;
    border: 5px solid white;
    margin-bottom: 20px;
}

.profile-name {
    font-size: 35px;
    color: #fff;
    margin-bottom: 15px;
}

.profile-detail {
    font-size: 18px;
    color: #f0f0f0;
    margin: 10px 0;
}

.back-btn {
    display: inline-block;
    background-color: #ffffff;
    color: #ff6a00;
    padding: 12px 25px;
    margin-top: 20px;
    text-decoration: none;
    border-radius: 25px;
    font-weight: bold;
    transition: background-color 0.3s ease, color 0.3s ease;
}

.back-btn:hover {
    background-color: #ffb199;
    color: white;
}

/* Responsive Design */
@media (max-width: 768px) {
    .profiles {
        grid-template-columns: 1fr; /* One profile per row on smaller screens */
    }

    .profile-detail-card {
        width: 90%;
        height: auto;
    }

    .profile-detail-img img {
        width: 200px;
        height: 200px;
    }
}




/* General Styles */
body {
    font-family: 'Arial', sans-serif;
    margin: 0;
    padding: 0;
    background: #f4f6f9;
    color: #333;
}

/* Main Container */
.container {
    display: flex;
    justify-content: center;
    align-items: center;
    padding: 20px;
    background: linear-gradient(135deg, #9eafff, #d0eaff);
    min-height: 100vh;
}

/* Profile Cards */
.profiles {
    display: grid;
    grid-template-columns: repeat(2, 1fr); /* Two profiles per row */
    gap: 30px;
    width: 100%;
    max-width: 1400px;
    height: auto;
    padding: 20px;
}

/* Profile Card */
.profile-card {
    background: linear-gradient(135deg, #ff8a00, #e52e71);
    border-radius: 15px;
    box-shadow: 0 15px 25px rgba(0, 0, 0, 0.1);
    padding: 30px;
    text-align: center;
    transition: transform 0.3s ease-in-out;
}

.profile-card:hover {
    transform: translateY(-20px);
}

.profile-img img {
    width: 250px;
    height: 250px;
    object-fit: cover;
    border-radius: 50%;
    border: 5px solid #fff;
}

.profile-info {
    padding: 20px 0;
}

.profile-info h2 {
    font-size: 30px;
    margin: 10px 0;
    color: #fff;
}

.profile-info p {
    font-size: 18px;
    color: #f0f0f0;
    margin: 10px 0;
}

.show-more-btn {
    display: inline-block;
    background-color: #ffffff;
    color: #e52e71;
    padding: 12px 25px;
    margin-top: 20px;
    text-decoration: none;
    border-radius: 25px;
    font-weight: bold;
    transition: background-color 0.3s ease, color 0.3s ease;
}

.show-more-btn:hover {
    background-color: #ff8a00;
    color: white;
}

/* Profile Detail Page */
.profile-detail-container {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    background: linear-gradient(135deg, #9eafff, #d0eaff);
    padding: 20px;
}

.profile-detail-card {
    background: linear-gradient(135deg, #ff8a00, #e52e71);
    border-radius: 15px;
    box-shadow: 0 15px 25px rgba(0, 0, 0, 0.2);
    padding: 40px 50px;
    text-align: center;
    width: 400px;
    height: 550px;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    transition: transform 0.3s ease-in-out;
}

.profile-detail-card:hover {
    transform: scale(1.05);
}

.profile-detail-img img {
    width: 250px;
    height: 250px;
    object-fit: cover;
    border-radius: 50%;
    border: 5px solid white;
    margin-bottom: 20px;
}

.profile-name {
    font-size: 35px;
    color: #fff;
    margin-bottom: 15px;
}

.profile-detail {
    font-size: 18px;
    color: #f0f0f0;
    margin: 10px 0;
}

.back-btn {
    display: inline-block;
    background-color: #ffffff;
    color: #e52e71;
    padding: 12px 25px;
    margin-top: 20px;
    text-decoration: none;
    border-radius: 25px;
    font-weight: bold;
    transition: background-color 0.3s ease, color 0.3s ease;
}

.back-btn:hover {
    background-color: #ff8a00;
    color: white;
}

/* Responsive Design */
@media (max-width: 768px) {
    .profiles {
        grid-template-columns: 1fr; /* One profile per row on smaller screens */
    }

    .profile-detail-card {
        width: 90%;
        height: auto;
    }

    .profile-detail-img img {
        width: 200px;
        height: 200px;
    }
}



/* General Styles */
body {
    font-family: 'Arial', sans-serif;
    margin: 0;
    padding: 0;
    background: #f1f9f9;
    color: #333;
}

/* Main Container */
.container {
    display: flex;
    justify-content: center;
    align-items: center;
    padding: 20px;
    background: linear-gradient(135deg, #00bcd4, #ff7043);
    min-height: 100vh;
}

/* Profile Cards */
.profiles {
    display: grid;
    grid-template-columns: repeat(2, 1fr); /* Two profiles per row */
    gap: 30px;
    width: 100%;
    max-width: 1400px;
    height: auto;
    padding: 20px;
}

/* Profile Card */
.profile-card {
    background: linear-gradient(135deg, #ff7043, #ffd54f);
    border-radius: 15px;
    box-shadow: 0 15px 25px rgba(0, 0, 0, 0.1);
    padding: 30px;
    text-align: center;
    transition: transform 0.3s ease-in-out;
}

.profile-card:hover {
    transform: translateY(-20px);
}

.profile-img img {
    width: 250px;
    height: 250px;
    object-fit: cover;
    border-radius: 50%;
    border: 5px solid #fff;
}

.profile-info {
    padding: 20px 0;
}

.profile-info h2 {
    font-size: 30px;
    margin: 10px 0;
    color: #fff;
}

.profile-info p {
    font-size: 18px;
    color: #f0f0f0;
    margin: 10px 0;
}

.show-more-btn {
    display: inline-block;
    background-color: #fff;
    color: #ff7043;
    padding: 12px 25px;
    margin-top: 20px;
    text-decoration: none;
    border-radius: 25px;
    font-weight: bold;
    transition: background-color 0.3s ease, color 0.3s ease;
}

.show-more-btn:hover {
    background-color: #ff7043;
    color: white;
}

/* Profile Detail Page */
.profile-detail-container {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    background: linear-gradient(135deg, #00bcd4, #ff7043);
    padding: 20px;
}

.profile-detail-card {
    background: linear-gradient(135deg, #ff7043, #ffd54f);
    border-radius: 15px;
    box-shadow: 0 15px 25px rgba(0, 0, 0, 0.2);
    padding: 40px 50px;
    text-align: center;
    width: 400px;
    height: 550px;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    transition: transform 0.3s ease-in-out;
}

.profile-detail-card:hover {
    transform: scale(1.05);
}

.profile-detail-img img {
    width: 250px;
    height: 250px;
    object-fit: cover;
    border-radius: 50%;
    border: 5px solid white;
    margin-bottom: 20px;
}

.profile-name {
    font-size: 35px;
    color: #fff;
    margin-bottom: 15px;
}

.profile-detail {
    font-size: 18px;
    color: #f0f0f0;
    margin: 10px 0;
}

.back-btn {
    display: inline-block;
    background-color: #fff;
    color: #ff7043;
    padding: 12px 25px;
    margin-top: 20px;
    text-decoration: none;
    border-radius: 25px;
    font-weight: bold;
    transition: background-color 0.3s ease, color 0.3s ease;
}

.back-btn:hover {
    background-color: #ff7043;
    color: #fff;
}

/* Responsive Design */
@media (max-width: 768px) {
    .profiles {
        grid-template-columns: 1fr; /* One profile per row on smaller screens */
    }

    .profile-detail-card {
        width: 90%;
        height: auto;
    }

    .profile-detail-img img {
        width: 200px;
        height: 200px;
    }
}






/* General Styles */
body {
    font-family: 'Arial', sans-serif;
    margin: 0;
    padding: 0;
    background: #f9f9f9;
    color: #333;
}

/* Main Container */
.container {
    display: flex;
    justify-content: center;
    align-items: center;
    padding: 20px;
    background: linear-gradient(135deg, #9c27b0, #00bfae);
    min-height: 100vh;
}

/* Profile Cards */
.profiles {
    display: grid;
    grid-template-columns: repeat(2, 1fr); /* Two profiles per row */
    gap: 30px;
    width: 100%;
    max-width: 1400px;
    height: auto;
    padding: 20px;
}

/* Profile Card */
.profile-card {
    background: linear-gradient(135deg, #9c27b0, #ffca28);
    border-radius: 15px;
    box-shadow: 0 15px 25px rgba(0, 0, 0, 0.1);
    padding: 30px;
    text-align: center;
    transition: transform 0.3s ease-in-out;
}

.profile-card:hover {
    transform: translateY(-20px);
}

.profile-img img {
    width: 250px;
    height: 250px;
    object-fit: cover;
    border-radius: 50%;
    border: 5px solid #fff;
}

.profile-info {
    padding: 20px 0;
}

.profile-info h2 {
    font-size: 30px;
    margin: 10px 0;
    color: #fff;
}

.profile-info p {
    font-size: 18px;
    color: #f0f0f0;
    margin: 10px 0;
}

.show-more-btn {
    display: inline-block;
    background-color: #fff;
    color: #9c27b0;
    padding: 12px 25px;
    margin-top: 20px;
    text-decoration: none;
    border-radius: 25px;
    font-weight: bold;
    transition: background-color 0.3s ease, color 0.3s ease;
}

.show-more-btn:hover {
    background-color: #9c27b0;
    color: white;
}

/* Profile Detail Page */
.profile-detail-container {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    background: linear-gradient(135deg, #9c27b0, #00bfae);
    padding: 20px;
}

.profile-detail-card {
    background: linear-gradient(135deg, #9c27b0, #ffca28);
    border-radius: 15px;
    box-shadow: 0 15px 25px rgba(0, 0, 0, 0.2);
    padding: 40px 50px;
    text-align: center;
    width: 400px;
    height: 550px;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    transition: transform 0.3s ease-in-out;
}

.profile-detail-card:hover {
    transform: scale(1.05);
}

.profile-detail-img img {
    width: 250px;
    height: 250px;
    object-fit: cover;
    border-radius: 50%;
    border: 5px solid white;
    margin-bottom: 20px;
}

.profile-name {
    font-size: 35px;
    color: #fff;
    margin-bottom: 15px;
}

.profile-detail {
    font-size: 18px;
    color: #f0f0f0;
    margin: 10px 0;
}

.back-btn {
    display: inline-block;
    background-color: #fff;
    color: #9c27b0;
    padding: 12px 25px;
    margin-top: 20px;
    text-decoration: none;
    border-radius: 25px;
    font-weight: bold;
    transition: background-color 0.3s ease, color 0.3s ease;
}

.back-btn:hover {
    background-color: #9c27b0;
    color: #fff;
}

/* Responsive Design */
@media (max-width: 768px) {
    .profiles {
        grid-template-columns: 1fr; /* One profile per row on smaller screens */
    }

    .profile-detail-card {
        width: 90%;
        height: auto;
    }

    .profile-detail-img img {
        width: 200px;
        height: 200px;
    }
}
